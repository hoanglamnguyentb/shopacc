@model List<Hinet.Model.Entities.DanhMucGame>

<ul class="list-unstyled">
    @foreach (var item in Model)
    {
        <li>
            <a href="/mua-acc/@item.Slug" class="text-decoration-none text-dark" target="_blank" rel="follow">
                @item.Name
            </a>
        </li>
    }
</ul>