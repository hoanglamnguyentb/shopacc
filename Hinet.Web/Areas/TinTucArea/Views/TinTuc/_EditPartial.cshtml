@using Hinet.Model.IdentityEntities
@using Hinet.Model.IdentityEntities
@using Hinet.Web.Areas.TinTucArea.Models

@model EditVM
@{

}
<div class="modal-dialog modal-full">
    @using (Ajax.BeginForm("Edit", "TinTuc", new { @area = "TinTucArea" }, new AjaxOptions()
    {
        HttpMethod = "POST",
        OnFailure = "AjaxFormError",
        OnSuccess = "AjaxFormSuccess",
    }, new { id = "editTinTucForm", enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(x => x.Id)
        @Html.HiddenFor(x => x.AnhBia)
        @Html.HiddenFor(x => x.ThoiGianXuatBan)
        @Html.HiddenFor(x => x.TacGia)
        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <div class="center">
                    <h4 class="modal-title">Cập nhật </h4>
                </div>
            </div>
            <div class="modal-body">
                <div class="form-horizontal">
                    <div class="form-group">
                        <label class="control-label col-sm-12">Ảnh </label>
                        <div class="col-sm-12">
                            <div class="image-upload-box">
                                <img id="previewAnh" src="@Model.AnhBia" alt="Preview ảnh" class="image-preview @(string.IsNullOrEmpty(Model.AnhBia) ? "" : "has-image")" />
                                <p id="uploadText" class="upload-text">+ Chọn ảnh</p>
                                <input type="file" id="uploadAnh" name="FileAnh" accept="image/*" class="file-input" />
                            </div>
                            @Html.HiddenFor(m => m.AnhBia, new { id = "DuongDanAnh" })
                            @Html.ValidationMessageFor(m => m.AnhBia, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label col-sm-12">Tiêu đề <span class="red">*</span></label>
                        <div class="col-sm-12">
                            @Html.TextBoxFor(m => m.TieuDe, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.TieuDe, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label col-sm-12">Nội dung </label>
                        <div class="col-sm-12">
                            <div id="editor">
                                @Html.Raw(Model.NoiDung)
                            </div>
                            @Html.HiddenFor(m => m.NoiDung, new { id = "NoiDung" })
                            @Html.ValidationMessageFor(m => m.NoiDung, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label col-sm-12">Trạng thái </label>
                        <div class="col-sm-12 w-full-select2">
                            @Html.DropDownList("TrangThai", ViewBag.DropdownListTrangThai as List<SelectListItem>, "-- Tất cả --", new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.TrangThai, "", new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer center">
                <button type="submit" class="btn btn-sm btn-primary">Hoàn thành</button>

                <button type="button" class="btn btn-sm btn-default" data-dismiss="modal">Đóng</button>
            </div>
        </div>
    }
</div>

<script>
    quill = new Quill('#editor', { theme: 'snow' });
    quill.root.innerHTML = '@Html.Raw(Model.NoiDung)';

    $("#editTinTucForm").on("submit", function () {
        var content = quill.root.innerHTML;
        $("#NoiDung").val(content);
    });

    $("#TrangThai").select2();

    $("#uploadAnh").change(function () {
        var input = this;
        if (input.files && input.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) {
                $("#previewAnh")
                    .attr("src", e.target.result)
                    .show();
            }
            reader.readAsDataURL(input.files[0]);;
        }
    });
</script>
@Scripts.Render("~/bundles/jqueryval")