@{
    ViewBag.Title = "Quản lý Khoa phòng";
    Layout = "~/Views/Shared/_LayoutAce.cshtml";
    ViewBag.ModuleName = "Quản lý Khoa phòng";
}
@using Hinet.Model.Entities;
@using Newtonsoft.Json;
@using Hinet.Service.Common;
@using Hinet.Web.Areas.DepartmentArea.Models;
@using Hinet.Service.DepartmentService.DTO;
@model PageListResultBO<DepartmentInfoDto>
@{
    var lstData = Html.Raw(JsonConvert.SerializeObject(Model.ListItem));
}

<style>
    .action-icon {
        margin-right: 5px;
    }

    .toUpper {
        text-transform: uppercase;
    }
</style>

@section ActionBarRight{
    <a href="#collapseDiv" class="btn btn-sm btn-primary" aria-controls="collapsePanel" data-toggle="collapse" role="button">
        <i class="fa fa-search"></i>  Tìm kiếm
    </a>
    @*<a href="javascript:void(0)" onclick="CreateAction('/DmDulieuDanhmucArea/DmDulieuDanhmuc/Create/@Model.GroupId')" class="btn btn-primary btn-sm"><i class="fa fa-plus"></i> Thêm mới</a>*@
}
@*@Html.Partial("_SearchPartial")*@

<div id="tbl-department" class="hntbl-cover">
    <div class="hntbl-cover-tableScroll">
        <table class="hinet-table table table-striped table-hover" cellspacing="0" width="100%">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Tên</th>
                    <th>Loại</th>
                    <th>Trạng thái</th>
                    @*<th></th>*@
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
</div>

@section scripts{
    <script>
        var lstData = eval(@lstData);
        var moduleTableSelector ="#tbl-department";
        function pagefunction(){
            var conf = [
                {
                    tdClass: "center",
                    isSort: false,
                    isCounter: true,
                    nameModel: "",
                    content: function (data) {
                        return "<input type='checkbox' />"
                    }
                },
                {
                    isSort: true,
                    nameModel: 'Name',
                    content: function (data) {
                        return data.Name
                    }
                },
                {
                    isSort: false,
                    nameModel: 'LoaiName',
                    content: function (data) {
                        return data.LoaiName
                    }
                },
                {
                    isSort: true,
                    nameModel: 'StatusName',
                    content: function (data) {
                        return data.StatusName
                    }
                },

                //{
                //    isSort: false,
                //    nameModel: "",
                //    tdClass: "",
                //    content: function (data) {
                //        var result = "<div class='btn-group' style='display:flex; justify-content:center;'>";
                //        result += "<a style='margin-right:5px;' class='btn btn-primary btn-sm' href='javascript:void(0)' onclick='EditAction(\"/DmDulieuDanhmucArea/DmDulieuDanhmuc/Edit/" + data.Id + "\")'   title = 'Chỉnh sửa'><i class='glyphicon glyphicon-edit action-icon'></i>Chỉnh sửa</a>";
                //        result += "<a class='btn btn-danger btn-sm' href='javascript:void(0)' onclick='onDelete(\"/DmDulieuDanhmucArea/DmDulieuDanhmuc/Delete/" + data.Id + "\")'  title = 'Xóa'><i class=' glyphicon glyphicon-remove action-icon'></i>Xoá</a>";
                //        result += "</div>";
                //        return result;
                //    }
                //},
            ];

            var getdatafunc=function(page,sortQuery,pageSize){
                $.ajax({
                    url: '/DepartmentArea/DepartmentTypeAndState/GetData',
                    type: 'post',
                    cache: false,
                    data: { "state":'@ViewBag.Datastate',"type":'@ViewBag.Datatype', "indexPage": page, "sortQuery": sortQuery, "pageSize": pageSize},
                    success: function (data) {
                        $(moduleTableSelector).hinetTable("data",{
                            pageSize:pageSize!=-1?pageSize:data.Count,
                            pageIndex:page,
                            pagecount: data.TotalPage,
                            recordCount: data.Count,
                            listItem: data.ListItem,});
                    },
                    error: function (err) {
                        CommonJS.alert(xhr.responseText);
                    }
                });

            }

            var tblData = $(moduleTableSelector).hinetTable("init",{
                pageSizeList: {
                    size: [10,20, 50, 100, -1], label: ['10','20','50','100','Tất cả']},
                pagecount: @Model.TotalPage,
                recordCount: @Model.Count,
                getData:getdatafunc,
                listItem:lstData,
                config: conf,
                showCheckBox: false
            });

        }

        function AfterSussessActionAjaxform(){
            $(moduleTableSelector).hinetTable("reload");
        }
        function AjaxSearchSuccess(rs) {
            $(moduleTableSelector).hinetTable("data", {
                pageIndex: 1,
                pagecount: rs.TotalPage,
                recordCount: rs.Count,
                listItem: rs.ListItem,
            });
        }
        pagefunction();
    </script>
}